# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in omniauth-firebase-id-token.gemspec
gemspec

group :test, :development do
  gem 'firebase_id_token'
  gem 'jwt' # For testing
  gem 'multi_json' # For testing
  gem 'omniauth'
end
